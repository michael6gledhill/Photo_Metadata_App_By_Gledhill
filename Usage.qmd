---
title: "How to Use"
subtitle: "Photo Metadata Editor - Main Features"
format:
  html:
    toc: true
    number-sections: false
    code-copy: true
    css: styles.css
execute: 
  echo: false
---

# How to Use

## Launch the App

```bash
python3 photo_meta_editor.py
```

The app window opens with 4 main sections:

1. **File List** (top) - Photos you're working with
2. **Operations** (left) - Buttons for actions
3. **Templates/Naming** (right) - Lists of your saved templates
4. **Preview** (bottom) - Shows new filenames

## Basic Workflow

### Step 1: Add Photos

Click **Open Files** to select images, or drag files directly into the window.

You can select multiple photos at once.

### Step 2: View Metadata

Select a photo and click **View Metadata** to see all EXIF and XMP tags currently on it.

A dialog shows all metadata in a table. This is read-only—it just shows what's there.

### Step 3: Apply Metadata

1. Select one or more photos
2. Choose a template from the **Template** dropdown (or create one)
3. Click **Apply Template**

The template's metadata is added to all selected photos.

Use **Merge Mode** (checkbox) to add without replacing existing metadata.

### Step 4: Delete Metadata (Optional)

Select photos and click **Delete Metadata** to remove all EXIF and XMP tags.

Useful if you want to completely clean metadata from photos.

### Step 5: Rename Files

1. Select one or more photos
2. Choose a naming convention from the **Naming** dropdown (or create one)
3. Click **Preview New Names** to see how they'll be renamed
4. Click **Rename Files** to apply

The files are renamed based on your pattern.

## Managing Templates

### Create a Template

Click **Create Template** → Enter name → Add EXIF/XMP fields → Click **Save Template**

### Import a Template

You can import pre-made templates from JSON files or by copying and pasting.

**Method 1: Import from File**

1. Click **Import** button (under Templates list)
2. Click **Select JSON File...**
3. Browse to a `.json` template file (see `example_templates/` folder in the GitHub repo)
4. Click **Import**

**Method 2: Copy & Paste**

1. Copy a template's JSON from GitHub (see [example templates](https://github.com/michael6gledhill/Photo_Metadata_App_By_Gledhill/tree/main/example_templates))
2. Click **Import** button
3. Paste the JSON in the text area
4. Click **Import**

**Available Example Templates:**
- `portrait_professional.json` - Professional portraits with copyright
- `travel_photography.json` - Travel photos with location fields
- `wedding_photography.json` - Wedding events with client licensing
- `event_photography.json` - General event documentation
- `stock_photography.json` - Stock photos with extensive keywords

### Edit a Template

Select template → Click **Edit** → Change fields → Click **Save Template**

### Delete a Template

Select template → Click **Remove** → Confirm

## Managing Naming Conventions

### Create a Naming Convention

Click **Create Naming Convention** → Enter name → Enter pattern → Click **Save Convention**

### Import a Naming Convention

Just like templates, you can import naming conventions from JSON files.

**Method 1: Import from File**

1. Click **Import** button (under Naming Conventions list)
2. Click **Select JSON File...**
3. Browse to a `.json` naming convention file
4. Click **Import**

**Method 2: Copy & Paste**

1. Copy a naming convention's JSON from GitHub
2. Click **Import** button
3. Paste the JSON in the text area
4. Click **Import**

**Available Example Naming Conventions:**
- `date_sequence.json` - Date with 4-digit sequence (e.g., `2025-12-08_0001.jpg`)
- `timestamp_camera.json` - Full timestamp + camera model
- `user_date_title.json` - Username + date + photo title
- `original_sequence.json` - Original filename + sequence
- `yearmonth_title_seq.json` - Year-month grouping + title + sequence

### Edit a Naming Convention

Select convention → Click **Edit** → Change pattern → Click **Save Convention**

### Delete a Naming Convention

Select convention → Click **Remove** → Confirm

## Useful Options

### Dry Run

Check **Dry run** (checkbox) to preview all changes without applying them.

Use before renaming to verify the new names look right.

### Merge Mode

Check **Merge metadata** to add template data without removing existing tags.

Uncheck to replace all metadata with the template.

## Undo

Click **Undo Last Operation** to revert your most recent action.

Works for both metadata changes and file renames.

## Keyboard Shortcuts

| Action | Shortcut |
|--------|----------|
| Open Files | Ctrl/Cmd + O |
| Rename Files | Ctrl/Cmd + R |
| Apply Template | Ctrl/Cmd + T |
| View Metadata | Ctrl/Cmd + M |
| Undo | Ctrl/Cmd + Z |
| Clear Files | Ctrl/Cmd + L |
| Exit | Ctrl/Cmd + Q |

## Clear Selection

Click **Clear Selection** (or Ctrl/Cmd + L) to remove all files from the list.

## Status Bar

The bottom status bar shows:
- Number of files selected
- Last action performed
- Metadata tool being used

## Tips

1. **Always preview first** - Click "Preview New Names" before renaming
2. **Use templates** - Create templates for different photo types (portraits, travel, events)
3. **Import pre-made templates** - Check the `example_templates/` folder on GitHub for ready-to-use templates
4. **Test with small batches** - Try with 3-5 photos first
5. **Back up important photos** - Keep originals safe before renaming
6. **Use meaningful names** - Include date or project info in naming patterns
7. **Copy from GitHub** - You can copy JSON templates directly from the GitHub repo and paste them into the Import dialog

## Quick Import Example

To use a GitHub template:

1. Visit [example_templates folder](https://github.com/michael6gledhill/Photo_Metadata_App_By_Gledhill/tree/main/example_templates)
2. Click on a template file (e.g., `portrait_professional.json`)
3. Click the "Raw" button
4. Copy all the text (Ctrl/Cmd + A, then Ctrl/Cmd + C)
5. In the app, click **Import** button
6. Paste into the text area (Ctrl/Cmd + V)
7. Click **Import**

Done! The template is now available in your templates list.

## Getting Help

- **View Metadata** dialog - Shows exactly what metadata is on a photo
- **Preview New Names** - Always check this before renaming
- **Dry Run** mode - Lets you see what will happen without applying

## More Information

- [Installation](Install.qmd) - Setup and requirements
- [Metadata Guide](Metadata.qmd) - EXIF and XMP fields
- [Naming Conventions](Naming.qmd) - Naming patterns and tokens
