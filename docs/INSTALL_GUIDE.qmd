---
title: "Photo Metadata Editor - Installation Guide"
author: "Michael Gledhill"
format:
  html:
    toc: true
    toc-depth: 3
    code-copy: true
    theme: cosmo
  pdf:
    toc: true
    number-sections: true
---

# Introduction

This guide will walk you through installing the Photo Metadata Editor application with simple, non-GUI methods:

1. **Command Line (macOS)** — One-line installers for Apple Silicon and Intel
2. **Windows (PowerShell)** — One command to install and run
3. **Linux** — Prerequisites + clone + run
4. **Manual Installation (advanced)** — Step-by-step for custom setups

---

# Method 1: Automatic Terminal Installation (macOS)

For users who prefer the command line without a GUI.

## Step 1: Open Terminal

**macOS:**
1. Press **Command (⌘) + Space**
2. Type: `terminal`
3. Press **Enter**

**Linux:**
Open any terminal emulator

## Step 2: Choose the Right Command

**Apple Silicon (M1/M2/M3):**
```bash
curl -fsSL https://raw.githubusercontent.com/michael6gledhill/Photo_Metadata_App_By_Gledhill/main/install_m1.py | python3
```

**Intel macOS:**
```bash
sudo curl -fsSL https://raw.githubusercontent.com/michael6gledhill/Photo_Metadata_App_By_Gledhill/main/Install.sh | bash
```

## Step 3: Paste and Run

1. Click in the Terminal window
2. Paste the command (right-click → Paste, or Command+V on macOS)
3. Press **Enter**

## Step 4: Watch Installation Progress

You'll see messages showing each step:

```
========================================
Photo Metadata Editor - Installation
========================================

✓ Homebrew already installed
✓ Git already installed
✓ Python 3 already installed
Cloning repository...
Installing Python dependencies...
```

## Step 5: Installation Complete

When you see:
```
✓ Installation Complete!
```

The app is ready to use!

# Method 2: Windows (PowerShell)

### Step 1: Open PowerShell (Administrator)
1. Right-click PowerShell
2. Select "Run as administrator"

### Step 2: Run the Installer
```powershell
Set-ExecutionPolicy Bypass -Scope Process -Force; iex ((New-Object System.Net.WebClient).DownloadString('https://raw.githubusercontent.com/michael6gledhill/Photo_Metadata_App_By_Gledhill/main/Install.ps1'))
```

:::{.callout-note}
Installs dependencies and launches the app.
:::

---

# Method 3: Linux (Ubuntu/Debian)

### Step 1: Install Prerequisites
```bash
sudo apt-get install -y git python3 python3-pip
```

### Step 2: Clone and Run
```bash
git clone https://github.com/michael6gledhill/Photo_Metadata_App_By_Gledhill.git
cd Photo_Metadata_App_By_Gledhill
python3 -m pip install -r requirements.txt
python3 main.py
```

---

# Method 4: Manual Installation (Advanced)

This method gives you more control over each step of the installation process.

## Prerequisites

Before we begin, make sure you have:

- macOS 10.15 (Catalina) or later
- At least 500 MB of free disk space
- Administrator access to your Mac

## Step 1: Open Terminal

1. Press **Command (⌘) + Space**
2. Type: `terminal`
3. Press **Enter**

## Step 2: Install Homebrew (if needed)

Homebrew is a package manager that makes installing software easier.

**Check if you have it:**
```bash
brew --version
```

If you see a version number, skip to Step 3.

**If not, install it:**
```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

Follow the on-screen instructions. You may need to enter your Mac password.

## Step 3: Install Git and Python

**Install Git:**
```bash
brew install git
```

**Install Python 3:**
```bash
brew install python@3
```

**Verify installations:**
```bash
git --version
python3 --version
```

You should see version numbers for both.

## Step 4: Choose Your Installation Location

Decide where you want to download the app files. We recommend your home directory:

```bash
cd ~
mkdir -p App
cd App
```

:::{.callout-note}
## What these commands do:
- `cd ~` - Go to your home folder
- `mkdir -p App` - Create a folder called "App"
- `cd App` - Go into that folder
:::

## Step 5: Clone the Repository

**If this is your first time installing:**
```bash
git clone https://github.com/michael6gledhill/Photo_Metadata_App_By_Gledhill.git
```

**If you already installed before and want to update:**
```bash
cd Photo_Metadata_App_By_Gledhill
git pull
```

## Step 6: Navigate to the Project

```bash
cd Photo_Metadata_App_By_Gledhill
```

You should now be inside the project folder.

## Step 7: Install Python Dependencies

First, upgrade pip (Python's package installer):
```bash
python3 -m pip install --upgrade pip
```

Then install the required packages:
```bash
python3 -m pip install -r requirements.txt
```

This installs:
- **PySide6** - GUI framework
- **Pillow** - Image processing
- **piexif** - Metadata handling

:::{.callout-note}
For macOS app bundling, use the provided installers above (Apple Silicon or Intel). Manual bundling is optional and not required for normal use.
:::

---

# Troubleshooting

## Command Not Found Errors

If you get `command not found` errors:

**For brew:**
```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

**For python3:**
```bash
brew install python@3
```

**For git:**
```bash
brew install git
```

## Permission Denied Errors

If you get permission errors during installation:

```bash
sudo chown -R $(whoami) ~/App
```

Then try the installation again.

## Build Fails

If `python3 setup.py py2app` fails:

**Clean previous builds:**
```bash
rm -rf build dist
python3 setup.py py2app
```

**Check Python version:**
```bash
python3 --version
```

You need Python 3.8 or later.

## App Won't Open

**Error: "App is damaged"**

This happens with unsigned apps. Fix it:
```bash
xattr -cr "/Applications/Photo Metadata Editor.app"
```

**Error: "Unidentified developer"**

1. Right-click the app
2. Click **Open**
3. Click **Open** in the dialog

## Dependencies Not Found

If the app opens but says packages are missing:

```bash
cd ~/App/Photo_Metadata_App_By_Gledhill
python3 -m pip install -r requirements.txt --force-reinstall
python3 setup.py py2app
cp -R "dist/Photo Metadata Editor.app" /Applications/
```

---

# Updating the App

To get the latest version:

## Automatic Method

Just run the install command again:
```bash
curl -fsSL https://raw.githubusercontent.com/michael6gledhill/Photo_Metadata_App_By_Gledhill/main/Install.sh | bash
```

## Manual Method

```bash
cd ~/App/Photo_Metadata_App_By_Gledhill
git pull
python3 -m pip install -r requirements.txt --upgrade
python3 setup.py py2app
rm -rf "/Applications/Photo Metadata Editor.app"
cp -R "dist/Photo Metadata Editor.app" /Applications/
```

---

# Updating the App

The Photo Metadata Editor includes built-in update checking!

## Automatic Updates

When you launch the app, it automatically checks for updates in the background. If an update is available:

1. A button in the toolbar will show **✨ Update Available (version)**
2. Click the button to install the update
3. The app will close and restart automatically with the latest version

## Manual Update Check

You can also manually check for updates:

1. Click the **🔄 Check for Updates** button in the toolbar
2. The app will check for the latest version
3. If an update is available, it will appear in the toolbar

## Update Process

When you click to install an update:

1. The latest code is downloaded from GitHub
2. Dependencies are updated
3. The app closes
4. A new instance with the latest version starts automatically

---

# Uninstalling

To remove the app:

**Remove the application:**
```bash
rm -rf "/Applications/Photo Metadata Editor.app"
```

**Remove source files (optional):**
```bash
rm -rf ~/App/Photo_Metadata_App_By_Gledhill
```

**Remove all project files (optional):**
```bash
rm -rf ~/App
```

---

# Getting Help

If you encounter issues not covered in this guide:

1. Check the [GitHub Issues](https://github.com/michael6gledhill/Photo_Metadata_App_By_Gledhill/issues)
2. Search for your error message
3. Open a new issue if needed

---

# Summary

## Quick Reference - Automatic Install
```bash
# One command to install everything
curl -fsSL https://raw.githubusercontent.com/michael6gledhill/Photo_Metadata_App_By_Gledhill/main/Install.sh | bash
```

## Quick Reference - Manual Install
```bash
# Install prerequisites
brew install git python@3

# Clone repository
cd ~/App
git clone https://github.com/michael6gledhill/Photo_Metadata_App_By_Gledhill.git
cd Photo_Metadata_App_By_Gledhill

# Install dependencies
python3 -m pip install -r requirements.txt
python3 -m pip install py2app

# Build and install
python3 setup.py py2app
cp -R "dist/Photo Metadata Editor.app" /Applications/

# Launch
open "/Applications/Photo Metadata Editor.app"
```

---

:::{.callout-tip}
## Pro Tip
Bookmark this guide for future reference! You can access it anytime at:
[https://github.com/michael6gledhill/Photo_Metadata_App_By_Gledhill](https://github.com/michael6gledhill/Photo_Metadata_App_By_Gledhill)
:::
